<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/colorAccent" />
    <stroke
        android:width="0dp"
        android:color="@android:color/transparent" />
    <corners
        android:bottomLeftRadius="@dimen/smallRadius"
        android:bottomRightRadius="@dimen/smallRadius"
        android:topLeftRadius="@dimen/smallRadius"
        android:topRightRadius="@dimen/smallRadius" />
</shape>